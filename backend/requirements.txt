# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0          # async PostgreSQL driver
alembic==1.14.0

# Validation & settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0   # validates email fields in Pydantic

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1             # passlib 1.7.4 is incompatible with bcrypt>=4.1

# File uploads
python-multipart==0.0.17

# Background tasks & email (Phase 4 — install now, configure later)
celery==5.4.0
redis==5.2.1
fastapi-mail==1.4.1
jinja2==3.1.4

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1            # async test client for FastAPI
